## This repository projects

**master** -> the main branch  
**small-authorization-project** -> a small REST NestJS‑project with JWT authorization and storing users in DB Postgres via Prisma  
**graphql-project** -> a small GraphQL NestJS‑project with JWT authorization and storing users in DB Postgres via Prisma
